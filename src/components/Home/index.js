import React from "react";
import Feed from "../Feed";

const Home = () => {
  return (
    <sectionn className="container mainContainer">
      <Feed />
    </sectionn>
  );
};

export default Home;
